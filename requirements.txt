# Core — needed by every template
scikit-learn>=1.0
numpy>=1.20
pandas>=1.3

# Plotting — used by the clustering and PCA templates
matplotlib>=3.4

# Hierarchical clustering dendrograms
scipy>=1.7

# NLP template (optional — it falls back to a built-in stop-word list without it)
nltk>=3.6
